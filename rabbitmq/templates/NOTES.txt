
** Please be patient while the chart is being deployed **

Credentials:

    echo "Username      :  default : rabbitmq"
    echo "Password      :  default : MySecurePass"
    echo "ErLang Cookie :   default : erlang-cookie=c-is-for-cookie-thats-good-enough-for-me"

To access for outside the cluster, perform the following steps:
