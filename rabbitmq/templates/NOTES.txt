1. Get the application URL by running these commands akhil test 3 :

{{- if contains "ClusterIP" .Values.service.type }}
  export SERVICE_URL=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "rabbitmq.fullname" . }} -o jsonpath='{.metadata.name}')
  echo "You have to configure your consumer with : http://${SERVICE_URL}:{{ $.Values.service.port }}"
{{- end }}